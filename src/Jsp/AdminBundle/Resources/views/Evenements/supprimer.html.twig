{% extends "JspAdminBundle::layout.html.twig" %}

{% block corps %}
<h1>EVENEMENTS</h1>
<div class="panel panel-danger">
  <div class="panel-heading">
    <h3 class="panel-title">Voulez vous vraiment supprimer l'événement ?</h3>
  </div>
  <div class="panel-body">
    <p>{{ evenement.titre }} </p>
	<div class="btn-group">
		<a href="{{ path('jsp_admin_evenements_liste') }}" class="btn btn-success">Retour</a>
		<a href="{{ path('jsp_admin_evenements_supprimerok', { 'id': evenement.id }) }}" class="btn btn-danger">Supprimer</a>
	</div>
  </div>
</div>
{% endblock %}
